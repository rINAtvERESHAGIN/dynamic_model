from django.urls import path
from .views import ListenSignal, CallbackSignal, CheckObjectExistView, GetRelatedObjectView

app_name = 'core'

urlpatterns = [
    path('signal/reception/', ListenSignal.as_view()),
    path('signal/callback/', CallbackSignal.as_view()),
    path('instance/related/exist/', CheckObjectExistView.as_view()),
    path('instance/related/get/', GetRelatedObjectView.as_view()),
    # path('listen/signal/', ),
]