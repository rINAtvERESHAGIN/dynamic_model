from rest_framework.routers import DefaultRouter, SimpleRouter, Route


class ExtendedRouter(DefaultRouter):
    def extend(self, router):
        for url in router.registry:
            self.register(*url)


